.venv/
node_modules/
__pycache__/

# `tree-sitter generate` writes all of this from grammar.js, in about half a
# second for the largest grammar here. Committing it would mean every grammar
# edit carrying a rewritten parse table -- thousands of lines nobody reads --
# next to the ten that were actually written.
grammars/*/src/
